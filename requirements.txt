# --- core framework ---
fastapi==0.111.1
python-dotenv==1.0.1
httpx==0.27.0
python-multipart==0.0.9   # for file uploads (STT endpoint)

# --- STT / TTS stack ---
openai-whisper==20231117
google-cloud-texttospeech==2.16.3
# torch is required for whisper; install from https://pytorch.org if uv fails

# --- RAG stack ---
chromadb==1.1.0             # vector DB
sentence-transformers==2.7.0   # Hugging Face embeddings
langgraph==0.6.7               # orchestration framework

# --- database & payments ---
supabase==2.6.0
stripe==10.7.0
streamlit
