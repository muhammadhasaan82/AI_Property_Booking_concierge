"""
app/config/agent_policy_loader.py
----------------------------------
Loads agent_policy.yaml and exposes typed accessors for the Phase 3 policy
router. Pure data layer — no LLM calls, no business logic.
"""